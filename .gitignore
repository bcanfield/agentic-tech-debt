# OS junk
.DS_Store

# Python bytecode (plugin scripts are stdlib Python)
__pycache__/
*.pyc

# Playwright MCP traces left in the cwd during agent-driven authoring
.playwright-mcp/

# skill-eval scratch (iterations, benchmarks, snapshots) — the skills themselves are tracked
.claude/skills/write-article-workspace/
